# badge

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build badge` to build the library.

## Running unit tests

Run `nx test badge` to execute the unit tests via [Jest](https://jestjs.io).
